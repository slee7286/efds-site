import Link from "next/link";
import { ArrowRight, BriefcaseBusiness, Compass, FileText, Target } from "lucide-react";

const areas = [["Finance", "Markets, valuation and the decisions behind capital."], ["Quant", "Build models that turn uncertainty into a usable signal."], ["Economics & policy", "Understand the incentives shaping the world around you."], ["Data & AI", "Use data to make complex systems easier to see."], ["Consulting", "Structure ambiguous questions and help teams move."], ["Software", "Ship the tools that make good analysis possible."]];

export default function CareersPage() { return <main><section className="container page-intro"><div className="eyebrow">EFDS careers</div><h1 className="display">Make the map<br />less blurry.</h1><p>Practical guides for finding the work that fits your questions — plus a private workspace for keeping the applications moving.</p><div className="hero-actions"><Link className="button button-primary" href="/login">Open member workspace <ArrowRight size={15} /></Link><Link className="button button-outline" href="#areas">Explore career areas</Link></div></section><section className="section-tight" id="areas"><div className="container"><div className="section-header"><div><div className="eyebrow">Career areas</div><h2 className="display">Start with a<br />direction.</h2></div><p>Guides are coming together around the routes EFDS members ask about most.</p></div><div className="pathway-grid" style={{ gridTemplateColumns: "repeat(3, 1fr)" }}>{areas.map(([title, text], index) => <div className="pathway surface" key={title}><span className="pathway-index">0{index + 1}</span><div style={{ color: "var(--coral)", marginTop: 24 }}>{index % 3 === 0 ? <BriefcaseBusiness size={22} /> : index % 3 === 1 ? <Target size={22} /> : <Compass size={22} />}</div><h3>{title}</h3><p>{text}</p><span className="pathway-link">Guide in progress <FileText size={13} /></span></div>)}</div></div></section></main>; }
