import { listRequirements } from "@/lib/db/knowledge";

export default async function RequirementsPage() { const requirements = await listRequirements(); return <div className="app-content"><div className="eyebrow">Structured knowledge</div><h1>Requirements.</h1><p className="app-subtitle">Conservative, source-linked candidates extracted by the EFDS knowledge-base.</p><section className="surface list-card">{requirements.map((item) => <article className="list-card-item" key={item.id}><div><span className="list-card-meta">{item.type} · {item.appliesTo ?? "General"}</span><h3>{item.text}</h3><p>Evidence retained in source article · {item.sourceArticleId}</p></div><span className={`badge ${item.reviewStatus === "approved" ? "badge-mint" : "badge-coral"}`}>{item.reviewStatus}</span></article>)}</section></div>; }
