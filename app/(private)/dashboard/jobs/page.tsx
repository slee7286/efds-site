import { Plus, SlidersHorizontal } from "lucide-react";
import type { TrackedJob } from "@/types/domain";

const jobs: TrackedJob[] = [{ company: "Northstar Capital", role: "Summer Analyst — Quantitative Research", careerArea: "Quant", location: "London", deadline: "22 Sep 2026", status: "preparing", nextAction: "Review probability notes" }, { company: "Atlas Policy Lab", role: "Research Intern", careerArea: "Economics", location: "London / hybrid", deadline: "04 Oct 2026", status: "interested", nextAction: "Read role profile" }, { company: "Fieldwork", role: "Data Science Intern", careerArea: "Data & AI", location: "London", deadline: "14 Oct 2026", status: "applied", nextAction: "Follow up 21 Sep" }];

export default function JobsPage() { return <div className="app-content"><div className="eyebrow">Career workspace</div><h1>Job tracker.</h1><p className="app-subtitle">A private place for opportunities, deadlines and the next useful action.</p><div className="filter-row"><div><span className="badge badge-neutral">3 opportunities</span><span className="badge badge-coral" style={{ marginLeft: 7 }}>2 closing soon</span></div><button className="button button-primary"><Plus size={14} /> Add opportunity</button></div><section className="surface list-card">{jobs.map((job) => <article className="list-card-item" key={`${job.company}-${job.role}`}><div><span className="list-card-meta">{job.company} · {job.careerArea}</span><h3>{job.role}</h3><p>{job.location} · Deadline {job.deadline ?? "none"} · Next: {job.nextAction}</p></div><span className={`badge ${job.status === "applied" ? "badge-mint" : "badge-coral"}`}>{job.status}</span></article>)}</section><div style={{ marginTop: 14 }}><button className="button button-quiet"><SlidersHorizontal size={14} /> Filter and sort</button></div></div>; }
