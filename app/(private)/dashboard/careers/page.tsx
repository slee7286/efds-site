import Link from "next/link";
import { ArrowRight, BookOpen, Compass, Target } from "lucide-react";

export default function PrivateCareersPage() { return <div className="app-content"><div className="eyebrow">Career workspace</div><h1>Make the next<br />move visible.</h1><p className="app-subtitle">Explore career directions, useful resources and practical next steps.</p><div className="pathway-grid" style={{ gridTemplateColumns: "repeat(3, 1fr)" }}><Card icon={<Compass size={20} />} title="Find your route" text="Compare finance, quant, economics, consulting, data and software paths." /><Card icon={<Target size={20} />} title="Build a plan" text="Turn a vague goal into the next two actions worth doing." /><Card icon={<BookOpen size={20} />} title="Keep learning" text="Explore guides and resources that help you develop your interests." /></div><div className="surface-dark info-card" style={{ marginTop: 16 }}><div className="eyebrow" style={{ color: "var(--mint)" }}>Your next step</div><h2>Start with one opportunity.</h2><p>Explore Imperial’s career support and the next steps available to you.</p><Link className="button button-primary" href="/careers" style={{ marginTop: 20 }}>Explore career directions <ArrowRight size={14} /></Link></div></div>; }
function Card({ icon, title, text }: { icon: React.ReactNode; title: string; text: string }) { return <article className="surface pathway"><div style={{ color: "var(--coral)" }}>{icon}</div><h3 style={{ marginTop: 52 }}>{title}</h3><p>{text}</p></article>; }
