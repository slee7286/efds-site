import Link from "next/link";
import { ArrowLeft, LockKeyhole } from "lucide-react";
import { LoginForm } from "@/components/auth/login-form";

export default async function LoginPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const params = await searchParams; const messages: Record<string, string> = { recovery_expired: "This sign-in link has expired or has already been used. Request a new email.", recovery_unavailable: "This recovery link is incomplete. Request a new email.", auth_failed: "We could not complete sign-in. Please try again." }; return <main className="auth-page"><section className="auth-aside"><Link className="brand" href="/"><span className="brand-mark">EFDS</span><span className="brand-copy">EFDS Society<small>Imperial College London</small></span></Link><div><div className="eyebrow">The private side</div><h1 className="display">Your next<br /><em>signal.</em></h1><p>A focused workspace for members, committee and the knowledge that keeps the society moving.</p></div><Link className="button button-quiet" style={{ color: "rgba(247,247,243,.7)" }} href="/"><ArrowLeft size={14} /> Back to public site</Link></section><section className="auth-panel"><div className="auth-box"><div className="eyebrow">Member access</div><h2>Sign in to EFDS.</h2><p>Imperial students and staff should sign in using their Imperial Microsoft account.</p><LoginForm initialMessage={params.error ? messages[params.error] : undefined} /><p className="auth-footnote"><LockKeyhole size={12} /> Access is checked after authentication. Imperial domains are allowed by default; approved external users need an active EFDS exception.</p></div></section></main>; }
